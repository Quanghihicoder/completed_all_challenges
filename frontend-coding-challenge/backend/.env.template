# Server Port
PORT=8000

# CORS - comma-separated list of allowed origins (e.g., frontend URLs)
ALLOW_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Example environment variable for other use
NODE_ENV=development